curl -sL https://deb.nodesource.com/setup_6.x | sudo -E bash -
sudo apt-get install -y nodejs
sudo npm install npm --global

sudo apt-get install zsh cowsay tmux htop python3-pip dtrx rubygems ruby-full fdupes cputool git
sudo apt install build-essential cmake python3-dev neofetch

pip install maybe glances
pip3 install numpy pandas jupyterlab flask advance-touch --user

npm install -g undollar

gem install colorls

curl https://sh.rustup.rs -sSf | sh
cargo build --release
cargo install bat
sudo apt-get install ripgrep
sudo curl https://raw.githubusercontent.com/mrowa44/emojify/master/emojify -o /usr/local/bin/emojify && chmod +x /usr/local/bin/emojify
sudo chsh -s $(which zsh) $USER
ln -s $HOME/dotfiles/* ~/
ln -s $HOME/dotfiles/.* ~/
fc-cache -fv ~/.fonts
rm ~/commands/motivate
ln -s $HOME/commands/motivate_folder/motivate ~/commands/motivate
~/.vim/bundle/YouCompleteMe/install.sh --clang-completer
rm ~/.git
